# Le scope @gamo vient de la forge ; tout le reste continue d'aller sur npmjs.
# Les paquets sont lisibles sans authentification : ne pas déclarer de jeton ici,
# un `_authToken` vide est refusé (401) là où l'absence d'en-tête passe.
@gamo:registry=https://forge.gamo.one/api/packages/Gamotune/npm/
